<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="30dp"
    tools:context=".Fragments.User.P_FindPerfilFragment">
    <TextView
        android:id="@+id/infos"
        android:text="@string/by"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="5dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:weightSum="10">
        <EditText
            android:id="@+id/editTextFind"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="9"
            android:ems="7"
            android:hint="@string/hint_vazio"
            android:imeOptions="actionDone"
            android:inputType="textPersonName"
            android:maxLength="10"
            android:selectAllOnFocus="true"
            android:textSize="14sp"
            tools:ignore="Autofill"
            tools:layout_editor_absoluteX="93dp"
            tools:layout_editor_absoluteY="100dp" />

        <Button
            android:id="@+id/btnFind"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="40dp"
            android:layout_height="52dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:backgroundTint="@android:color/transparent"
            app:icon="@drawable/find"
            app:iconSize="20dp"
            app:iconTint="#737373"
            tools:targetApi="lollipop" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/loadingSkins"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:maxWidth="30dip"
        android:minWidth="30dip"
        android:visibility="gone" />
    <TextView
        android:id="@+id/msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:textColor="@color/red"
        android:visibility="gone" />

    <GridView
        android:id="@+id/listSkins"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:columnWidth="@dimen/size"
        android:numColumns="auto_fit"
        tools:ignore="NestedScrolling" />

</LinearLayout>